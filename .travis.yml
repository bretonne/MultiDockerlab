sudo: required
services:
  - docker

before_install:
  - docker build -t bretonnetx/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run bretonnetx/react-test npm test -- --coverage

after_success:
  - docker build -t bretonnetx/multi-client ./client
  - docker build -t bretonnetx/multi-nginx ./nginx
  - docker build -t bretonnetx/multi-server ./server
  - docker build -t bretonnetx/multi-worker ./worker
# login to docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --pasword-stdin
# take those into dockerhub
  - docker push bretonnetx/multi-client
  - docker push bretonnetx/multi-nginx
  - docker push bretonnetx/multi-server
  - docker push bretonnetx/multi-worker